kind = "web"
previewPath = "/"
title = "Luxury Carpet Store"
version = "1.0.0"
id = "artifacts/carpet-store"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25491

[services.development]
run = "pnpm --filter @workspace/carpet-store run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/carpet-store", "run", "build" ]
publicDir = "artifacts/carpet-store/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25491"
BASE_PATH = "/"
